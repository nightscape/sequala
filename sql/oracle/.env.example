# Example .env file for sql/oracle Taskfile
#
# NOTE: For DevContainer users, .env is auto-mounted from .devcontainer/oracle.env
#       You only need to create .env manually when running outside DevContainer.
#
# Copy to .env and adjust values for your environment

# Set HOST_ORACLE_DIR to the host path that maps to /workspace/sql/oracle
# (Required when running Docker commands from inside a container)
# HOST_ORACLE_DIR=/Users/yourname/path/to/sequala/sql/oracle

# Override connection strings to use container names instead of localhost
# (Required when running from inside the Docker network)
# ORIG_CONN=system/orakel@//oracle-orig:1521/FREEPDB1
# NEW_CONN=system/orakel@//oracle-new:1521/FREEPDB1

# Optional: Override credentials
# ORACLE_PASSWORD=orakel
# APP_USER=xmdm_user
# APP_USER_PASSWORD=xmdm_pass
